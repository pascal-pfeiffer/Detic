[project]
name = "Detic"
version = "0.1.0"
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.8"
classifiers = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
